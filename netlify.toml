[build]
  command = "npm ci && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"
  NPM_FLAGS = "--no-audit --no-fund"
  CI = "false"

# SPA routing: React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
